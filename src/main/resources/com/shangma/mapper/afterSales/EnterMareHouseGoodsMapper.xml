<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shangma.mapper.afterSales.EnterMareHouseGoodsMapper">

    <select id="findOrderGoods" resultType="com.shangma.entity.afterSales.EnterMareHouseGoods">
        select t1.good_id,t3.brand_name,t2.goods_name,t2.goods_model,t2.goods_color,t1.order_id
        from t_b2c_order_goods t1,t_goods t2,t_goods_brand t3
        where t1.good_id=t2.id and t2.brand_id = t3.id and t1.good_id=#{goodId} and t1.order_id = #{orderId}

    </select>
    <select id="selectById" resultType="com.shangma.entity.afterSales.EnterMareHouseGoods">
        select *from enter_marehouse_goods where good_id=#{goodId}
    </select>
    <update id="updateByOne" parameterType="com.shangma.entity.afterSales.EnterMareHouseGoods">
        update enter_marehouse_goods set
        good_id=#{goodId},
        brand_name=#{brandName},
        goods_name=#{goodsName},
        goods_model=#{goodsModel},
        goods_color=#{goodsColor},
        goods_trace=#{goodsTrace},
        return_type=#{returnType},
        second_id=#{secondId},
        factory_address=#{factoryAddress},
        remark=#{remark},
        order_id=#{orderId}
    </update>


</mapper>